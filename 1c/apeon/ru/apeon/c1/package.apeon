package ru.apeon.c1

datasource c1

query Test {
`from MatMoveIn where number = "ТК000000001"`.select.foreach { doc =>
    doc.materials.foreach{
        _.quantity = _.number
    }
    doc.remark = "11111111111"
}

//`from MatMoveIn`.select

/*`from Material<c1>`.select.foreach {material =>
    material sync as s to comtec.Material<comtec.comtec> as d where `s.number = d.number and s.name = d.name`
	}*/
}
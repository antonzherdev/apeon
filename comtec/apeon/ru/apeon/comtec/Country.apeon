package ru.apeon.comtec

entity Country<comtec> {
    table dba.country

    column id integer primary key
    column name(nm) char(250)
    column shortName(nm_short) char(45)
    column code(kod) char(3)
	
	def toString : String = Format.str("Страна", id, name)
}
package ru.apeon.comtec

entity Currency<comtec> {
    table dba.currency

    column id integer primary key
    column name(nm) char(22)
	
	def toString : String = Format.str("Валюта", id, name)
}